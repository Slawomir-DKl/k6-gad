export enum AuthorizationType {
  regularUser = 1,
  adminUser = 2,
  noAuthorization = 3,
}

export enum OptionsType {
  constantVus = "constantVus",
  rampingVus = "rampingVus",
  runOnce = "runOnce",
}
